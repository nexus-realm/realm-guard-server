# Datasource Prometheus, provisionnée au démarrage de Grafana (P5).
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    uid: prometheus # référencé par les dashboards → stable, ne pas changer
    isDefault: true
    editable: false
